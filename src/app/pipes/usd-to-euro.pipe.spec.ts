import { UsdToEuroPipe } from './usd-to-euro.pipe';

describe('UsdToEuroPipe', () => {
  it('create an instance', () => {
    const pipe = new UsdToEuroPipe();
    expect(pipe).toBeTruthy();
  });
});
